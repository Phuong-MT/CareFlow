export enum commonEnum {
    ACTIVE = '1',
    INACTIVE = '0',
  }

  
  export enum RoleEnum {
    SUPER_ADMIN = 'super_admin',
    ADMIN = 'admin',
    OPERATOR = 'operator',
    USER = 'user',
    POC = 'poc'
  }

  export enum QueueEnum {
    PENDING = 'pending',
    SERVING = 'serving',
    SUCCESS = 'success',
    IS_CLOSED = 'is_closed',
  }